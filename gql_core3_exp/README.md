# graphql-core3

we have wrapper classes for each gql type in both the graphql module and the graphql.type submodule

how are interface types handled at the server, are the interface types’ resolvers inherited? are the resolvers meant
to be overwritten, how is the interface type enforced?
